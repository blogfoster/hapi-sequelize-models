{
  "name": "hapi-sequelize-models",
  "version": "0.0.1",
  "description": "hapi sequelize model loader plugin",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && babel -d ./dist ./source -s",
    "prepublish": "npm run build",
    "test": "mocha",
    "test:coverage": "rm -rf ./coverage && NODE_ENV=test babel-node node_modules/.bin/isparta cover --report html node_modules/.bin/_mocha",
    "eslint": "eslint .",
    "d:build": "docker-compose build && docker-compose run hapi-sequelize-models ':' && docker-compose up -d",
    "d:login": "docker-compose run hapi-sequelize-models /bin/bash",
    "d:test": "docker-compose run hapi-sequelize-models npm test",
    "d:clean": "docker-compose stop && docker-compose rm --all -fv"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blogfoster/hapi-sequelize-models.git"
  },
  "keywords": [
    "hapi",
    "sequelize",
    "models",
    "hapi-plugin"
  ],
  "author": "blogfoster GmbH",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/blogfoster/hapi-sequelize-models/issues"
  },
  "homepage": "https://github.com/blogfoster/hapi-sequelize-models#readme",
  "dependencies": {
    "joi": "^8.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-core": "^6.7.6",
    "babel-eslint": "^6.0.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "bluebird": "^3.3.5",
    "eslint": "^2.8.0",
    "eslint-config-airbnb": "^7.0.0",
    "eslint-plugin-jsx-a11y": "^0.6.2",
    "eslint-plugin-react": "^5.0.1",
    "expect": "^1.16.0",
    "hapi": "^13.3.0",
    "isparta": "^4.0.0",
    "mocha": "^2.4.5",
    "mysql": "^2.10.2",
    "sequelize": "^3.21.0",
    "sequelize-fixtures": "^0.5.1"
  }
}
